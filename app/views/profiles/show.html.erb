<div class="container">
  <h3>プロフィール</h3>

  <%= render 'commons/profile' %>

  <div class="actions">
    <%= link_to '編集', edit_profile_path, class: 'btn btn-primary' %>
    <%= link_to 'フォロー中', account_followings_path(account_id: @profile.user_id), class: 'btn btn-primary' %>
    <%= link_to 'フォロワー', account_followers_path(account_id: @profile.user_id), class: 'btn btn-primary' %>
  </div>
</div>